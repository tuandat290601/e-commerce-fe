import { IProductCard } from "@/interfaces/product.interface";
import React from "react";

const ProductList = (products: IProductCard[]) => {
  return <div>ProductList</div>;
};

export default ProductList;
